import { CountPanelLinksPipe } from './count-panel-links.pipe';

describe('CountPanelLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new CountPanelLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
